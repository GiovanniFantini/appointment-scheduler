name: Deploy Admin App to Azure Web App

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/admin-app/**'
      - '.github/workflows/deploy-admin-app.yml'
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy Admin App

    steps:
      - uses: actions/checkout@v3

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/admin-app/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend/admin-app
          npm ci

      - name: Build application
        run: |
          cd frontend/admin-app
          npm run build
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME_ADMIN }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_ADMIN }}
          package: frontend/admin-app

      - name: Configure Azure Web App Settings
        uses: azure/appservice-settings@v1
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME_ADMIN }}
          app-settings-json: |
            [
              {
                "name": "API_URL",
                "value": "${{ secrets.API_URL || 'https://appointment-scheduler-api.azurewebsites.net' }}",
                "slotSetting": false
              },
              {
                "name": "NODE_ENV",
                "value": "production",
                "slotSetting": false
              },
              {
                "name": "SCM_DO_BUILD_DURING_DEPLOYMENT",
                "value": "true",
                "slotSetting": false
              },
              {
                "name": "WEBSITE_NODE_DEFAULT_VERSION",
                "value": "~20",
                "slotSetting": false
              }
            ]

      - name: Set Linux Startup Command
        run: |
          az webapp config set \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP || 'appointment-scheduler-rg' }} \
            --name ${{ secrets.AZURE_WEBAPP_NAME_ADMIN }} \
            --startup-file "npm start"
