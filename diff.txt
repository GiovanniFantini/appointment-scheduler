diff --git a/frontend/admin-app/src/pages/LoginPage.tsx b/frontend/admin-app/src/pages/LoginPage.tsx
index ded16da..342153e 100644
--- a/frontend/admin-app/src/pages/LoginPage.tsx
+++ b/frontend/admin-app/src/pages/LoginPage.tsx
@@ -12,6 +12,8 @@ interface User {
   isConsumer: boolean
   isMerchant: boolean
   isEmployee: boolean
+  // 1 = admin, 2 = consumer, 3 = merchant, 4 = employee
+  accountType: number
 }
 
 interface LoginPageProps {
@@ -34,7 +36,8 @@ export default function LoginPage({ onLogin }: LoginPageProps) {
       const response = await apiClient.post('/auth/admin/login', { email, password })
       const { token, ...userData } = response.data as { token: string } & User
 
-      if (!userData.isAdmin) {
+
+      if (userData.accountType !== 1) {
         setError('Access reserved for administrators only.')
         return
       }
